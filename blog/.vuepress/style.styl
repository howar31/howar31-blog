@import "prism-monokai.styl"

$whiteColor = #FFFFFF;
$lightColor = #F4F4F3;
$darkColor = #272822;
$blackColor = #000000;

/**
 * Site
 */
body {
    background-color: $lightColor;
}

/**
 * Navbar
 */
.navbar {
    background-color: $darkColor;
    color: $lightColor;
    border-bottom: 0;
    box-shadow: 0 0 1px 0px $blackColor;
}
.navbar .links {
    background-color: $darkColor;
}
.navbar .site-name {
    color: $lightColor;
}
.nav-links a.router-link-active,
.nav-links a:hover {
    color: $whiteColor;
}
.dropdown-wrapper .nav-dropdown .dropdown-item {
    color: $darkColor;
}

/**
 * Sidebar
 */
.sidebar {
    background-color: $lightColor;
    border-right: 0;
}
@media (max-width: 719px) {
    .theme-container.sidebar-open .sidebar {
        background-color: $darkColor;
        color: $lightColor;
    }
    a.sidebar-link,
    .sidebar .nav-links a,
    .dropdown-wrapper .nav-dropdown .dropdown-item {
        color: $lightColor;
    }

    .nav-links a.router-link-active,
    .nav-links a:hover {
        color: $whiteColor;
    }

    .sidebar .nav-links {
        border-bottom: 1px solid $blackColor;
    }
}
/**
 * Page
 */
.content:not(.custom),
.page-edit,
.page-nav {
    max-width: 900px;
}